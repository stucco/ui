
span
  each key,index in Object.keys(model._values)
    if ((key !== "_id") && (key !== "description") && (key !== "vertexType") && (key !== "_type") && (key !== "inEdges") && (key !== "outEdges") && (key !== "name"))
      dt #{key}
      - if(key.indexOf("Date") !== -1){
      -   val = new Date(model[key]).toUTCString()
      - }else if(key.indexOf("Time") !== -1){
      -   val = new Date(parseInt(model[key])).toISOString()
      - }else{
      -   val = model[key];
      - }
      dd #{val}
